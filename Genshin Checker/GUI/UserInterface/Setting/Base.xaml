<UserControl x:Class="Genshin_Checker.GUI.UserInterface.Setting.Base"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:label="clr-namespace:Genshin_Checker.GUI.UserInterface.Component"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="960"
        Background="{DynamicResource BackgroundColor}"
        Foreground="{DynamicResource ForegroundColor}">

    <Grid>
        <Grid.Resources>
            <Style TargetType="label:CategoryLabel">
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Column="0" Grid.Row="1"  VerticalScrollBarVisibility="Auto">
            <StackPanel Background="#202442" >
                <label:CategoryLabel x:Name="Category_General" Click="CategoryClick" Tag="General" Text="全般" IsSelected="True"/>
                <label:CategoryLabel x:Name="Category_Notification" Click="CategoryClick" Tag="Notification" Text="通知" IsSelected="False"/>
                <label:CategoryLabel x:Name="Category_AuthApp" Click="CategoryClick" Tag="AuthApp" Text="アプリ連携" IsSelected="False"/>
                <label:CategoryLabel x:Name="Category_HoYoLabConfig" Click="CategoryClick" Tag="HoYoLabConfig" Text="HoYoLab" IsSelected="False"/>
                <label:CategoryLabel x:Name="Category_AppData" Click="CategoryClick" Tag="ApplicationData" Text="アプリデータ" IsSelected="False"/>
                <label:CategoryLabel x:Name="Category_VersionInfo" Click="CategoryClick" Tag="VersionInfo" Text="バージョン情報" IsSelected="False"/>
            </StackPanel>
        </ScrollViewer>
        <ScrollViewer Grid.Column="1" Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Frame x:Name="PageFrame" NavigationUIVisibility="Hidden"
                   ScrollViewer.VerticalScrollBarVisibility="Auto" 
                    ScrollViewer.CanContentScroll="True" />
        </ScrollViewer>
        <materialDesign:Snackbar x:Name="ErrorMessage"
            MessageQueue="{materialDesign:MessageQueue}"
            Foreground="Yellow"
            Grid.Row="1" Grid.Column="1"
            HorizontalAlignment="Center"
            HorizontalContentAlignment="Center"
            VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>